using HtmlAgilityPack;
using System;
using System.Collections.Generic;
using System.IO;
using System.Text;
using Zdaas.RFPBusinessModel;

namespace Zdaas.RFPManipulation
{
    public static class temp
    {
        public static void TempWirtefile(OpportunityEntity opportunityPublished)
        {
            //using (StreamWriter writer = new StreamWriter("c:\\Akmal\\important.txt"))
            //{
            //    foreach (var heading in opportunityPublished.OpportunityHeading)
            //    {
            //      HtmlNode htmlNode =  HtmlNode.CreateNode("<div>" + heading.Heading + "</div>");

            //        writer.WriteLine(heading.HeadingId + " : "  + heading.ParentHeadingId + " : " + htmlNode.InnerText.Trim());

            //        foreach (var content in heading.OpportunityContent)
            //        {
            //            HtmlNode htmlContent = HtmlNode.CreateNode("<div>" + content.OppContent + "</div>");
            //            writer.WriteLine("c  :  " + content.HeadingId + " : " + content.IndexNumber + " : " + htmlContent.InnerText.Trim());
            //        }
            //    }

                
               


            //}
        }
    }
}
