import { BridgeSynonymContractVehicleList } from './bridge-synonym-contract-vehicle-list';

describe('BridgeSynonymContractVehicleList', () => {
  it('should create an instance', () => {
    expect(new BridgeSynonymContractVehicleList()).toBeTruthy();
  });
});
