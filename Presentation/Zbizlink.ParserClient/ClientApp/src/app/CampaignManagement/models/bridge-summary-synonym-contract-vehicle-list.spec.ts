import { BridgeSummarySynonymContractVehicleList } from "./bridge-summary-synonym-contract-vehicle-list";
 
describe('BridgeSummarySynonymContractVehicleList', () => {
  it('should create an instance', () => {
    expect(new BridgeSummarySynonymContractVehicleList()).toBeTruthy();
  });
});
