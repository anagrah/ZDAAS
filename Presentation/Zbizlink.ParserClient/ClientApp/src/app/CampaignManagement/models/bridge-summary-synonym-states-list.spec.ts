 import { BridgeSummarySynonymStatesList } from './bridge-summary-synonym-states-list';

describe('BridgeSummarySynonymStatesList', () => {
  it('should create an instance', () => {
    expect(new BridgeSummarySynonymStatesList()).toBeTruthy();
  });
});
