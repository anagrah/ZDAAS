import { SummarySynonymBridges } from './summary-synonym-bridges';

describe('SummarySynonymBridges', () => {
  it('should create an instance', () => {
    expect(new SummarySynonymBridges()).toBeTruthy();
  });
});
