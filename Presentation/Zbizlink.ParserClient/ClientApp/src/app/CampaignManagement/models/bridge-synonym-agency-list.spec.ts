import { BridgeSynonymAgencyList } from './bridge-synonym-agency-list';

describe('BridgeSynonymAgencyList', () => {
  it('should create an instance', () => {
    expect(new BridgeSynonymAgencyList()).toBeTruthy();
  });
});
